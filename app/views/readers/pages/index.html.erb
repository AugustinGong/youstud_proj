<div class="row">
    <% @books.each do |book| %>
        <div class="col-md-6 mt-3 col-lg-4">
            <div class="card book-card">
                <div class="header-image" style="background-image: url(<%= url_for(book.header_image) %>)">
                </div>
                <div class="card-body d-flex flex-column">
                    <h5 class="card-title"><%= book.title %></h5>
                    <p class="card-text"><%= truncate(book.description, length: 300) %></p>
                    <%= link_to "Scopri di più", blog_book_path(book), class: 'btn btn-primary mt-auto d-grid gap-2 mt-3' %>
                </div>
            </div>
        </div>
    <% end %>
</div>